{
	"title": "Notice",
	"description": "A piece of information to be drawn to a user's attention",
	"type": "object",
	"properties": {
		"type": {
			"description": "The type of the notice",
			"type": "string",
			"enum": [
				"Warning",
				"Information",
				"Error"
			]
		},
		"message": {
			"description": "The content of the notice",
			"type": "string"
		}
	},
	"required": [
		"type",
		"message"
	]
}
