{
	"title": "Policy File",
	"description": "A file detailing a specific version of a policy",
	"type": "object",
	"properties": {
		"name": {
			"description": "The name of the file",
			"type": "string"
		},
		"format": {
			"description": "The format of the file",
			"type": "string",
			"enum": [
				"PDF (Text)",
				"PDF (Mixed)",
				"PDF (Image)"
			]
		},
		"provenance": {
			"description": "The details of the file's origin",
			"$ref": "provenance"
		},
		"version": {
			"description": "The version of this instance of the policy",
			"type": "string"
		},
		"duration": {
			"description": "When the policy was in effect",
			"$ref": "policy-file-duration"
		},
		"notices": {
			"description": "Any notices associated with this file",
			"type": "array",
			"minItems": 1,
			"items": {
				"$ref": "notice"
			}
		}
	},
	"required": [
		"name",
		"format",
		"provenance"
	]
}
