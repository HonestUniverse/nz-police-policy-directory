{
	"title": "Policy File Duration",
	"description": "Information on when a specific version of a policy was in effect",
	"type": "object",
	"properties": {
		"start": {
			"description": "The first day on which this version of the policy was in effect",
			"type": "string",
			"format": "date"
		},
		"end": {
			"description": "The last day on which this version of the policy was in effect",
			"type": "string",
			"format": "date"
		},
		"ended": {
			"description": "Whether or not this version of the policy is still in effect",
			"type": "boolean",
			"enum": [true]
		},
		"on": {
			"description": "One or more dates on which this version of the policy was in effect",
			"type": "array",
			"minItems": 1,
			"items": {
				"type": "string",
				"format": "date"
			}
		}
	},
	"anyOf": [
		{ "required": ["start"] },
		{
			"oneOf": [
				{ "required": ["end"] },
				{ "required": ["ended"] }
			]
		},
		{ "required": ["on"] }
	]
}
