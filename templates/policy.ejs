<!DOCTYPE html>
<html>
	<%- include('components/head', { title }); %>
	<body>
		<%- include('components/header') %>
		<main>
			<h2><%= title %></h2>
			<p><%= type %></p>
			<ol>
				<% files.forEach((file) => { %> <%- include('components/file', { file }) %> <% }) %>
			</ol>
		</main>
		<%- include('components/footer') %>
	</body>
</html>
