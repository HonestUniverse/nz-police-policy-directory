<!DOCTYPE html>
<html>
	<%- include('components/head', { title }); %>
	<body>
		<%- include('components/header') %>
		<main>
			<h2><%= title %></h2>
			<p><%= type %></p>
			<ol>
				<% versions.forEach((version) => { %> <%- include('components/version', { version }) %> <% }) %>
			</ol>
		</main>
		<%- include('components/footer') %>
	</body>
</html>
