<%#
	locals: {
		siteData: SiteData,
		pageData: {
			error: number,
			errorString: string,
		},
	}
%>

<%
	const {
		error,
		errorString,
	} = pageData;
%>

<%- include('../layout/head', {
	title: error,
}); %>

<div class="content">
	<p><%- errorString %></p>
</div>

<%- include('../layout/foot'); %>
